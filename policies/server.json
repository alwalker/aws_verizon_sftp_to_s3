{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Sid": "VisualEditor3",
            "Effect": "Allow",
            "Action": "s3:*",
            "Resource": [
                "arn:aws:s3:::${shot_bucket}",
                "arn:aws:s3:::${shot_bucket}/*",
                "arn:aws:s3:::${tracking_bucket}",
                "arn:aws:s3:::${tracking_bucket}/*"
            ]
        }
    ]
}